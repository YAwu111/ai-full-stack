# LocalStorage

- 本地？
    browser 
    会给我们一个空间，用于存储一些配置，永久存储（不会过期）
    key => value(存字符串) JSON.stringify()


- css 具有继承特性 inherit
    - font-size color 等 没有继承特性，收到要写到打斗
    - background, width,height 等不会继承

- outline overflow
    outline 轮廓 有点像border, 但不占据盒模型
    overflow 管住子元素超出父元素

- flex 
    - 新的格式化上下文